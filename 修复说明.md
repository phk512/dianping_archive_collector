# 修复说明：图片「上传者 / 发布时间」获取不准确

> **版本说明**：本文档描述的是 V3.0 引入的「上传者 / 发布时间」修复，V3.1 / V3.2 继续沿用。
> V3.1 新增「采集日期范围、自动获取商户名称、自动找店」；
> V3.2 新增「带登录态直接抓列表页/搜索页并自动翻页」，见 `新功能说明.md`。
> 脚本文件名：`dianping_archive_collector_v3_2.py`。

交付文件：

| 文件 | 说明 |
|---|---|
| `dianping_archive_collector_v3_2.py` | 脚本主体（V2.9 → V3.0 修元数据 → V3.1 加新功能 → V3.2 加登录态抓列表页） |
| `verify_v3.py` | 回归自检：`python verify_v3.py`（离线 54 项）/ `--live`（联网 85 项） |
| `verify_features.py` | 新功能自检（69 项，需联网） |
| `verify_login.py` | 登录态与列表页自检（34 项，需联网） |
| `verify_gui_cli.py` | 命令行子命令与界面装配自检 |
| `修复说明.md` | 本文档（元数据修复） |
| `新功能说明.md` | 新功能说明（日期范围 / 商户名称 / 自动找店 / 登录态抓列表页） |

---

## 一、旧版为什么会取错

旧版（V2.9）的 `extract_image_metadata()` 有三层，三层都不够可靠：

1. **第一层「向上找祖先 + 打分」**
   从每张 `<img>` 向上遍历最多 14 层父节点，谁「文本里有日期」就给谁打分（基础分 +12、含 `|` 加 5、类名含 photo/pic/… 加 4、图片数 ≤2 加 5，再扣深度和文本长度），取最高分节点当卡片。
   **问题**：一旦某个「包含多张图片」的容器得分更高，容器里所有图片都会拿到同一份作者/时间（通常是容器里最后一张图的那份）。表现就是「有些图的作者/时间是隔壁照片的」。

2. **BeautifulSoup 缺失时静默降级**
   整段解析被 `try: … except Exception: pass` 包住，没装 beautifulsoup4 时不会报错，直接只剩第三层的字符窗口兜底。

3. **第三层「图片 URL 前后 ±5000 字符窗口」**
   取图片地址前后共 1 万字符的可见文本，把窗口里**第一个**日期、以及它前面最后一段文字当作者。
   **问题**：窗口横跨好几张卡片，取到的往往是**上一张**照片的作者/时间 —— 这正是「数值看着合理但就是错的」的来源。

另外还有两个放大错误的设计：

4. **图片与元数据用 URL 字符串绑定**。同一张图换个尺寸后缀（`...jpg%40240w_180h_1e_1c_1l` / `...jpg@600w`）就对不上号，只能掉进第 3 条的乱猜路径。
5. **写库只「填空」**。`update_image_metadata` 只写空字段，所以一旦写错，**重爬也永远改不回来**。

还有一个隐性缺口：页面上只显示 `06-21` 这种月-日的照片，旧版原样存库，年份永久缺失。

---

## 二、实测的页面真实结构（2026-09 采样 5 家商户 6 个页面）

```
<li class="J_list">
  <div class="img">
    <a class="J_entry" href="/photos/7605587866" data-index="0">
      <img src="https://qcloud.dpfile.com/pc/<44位图片ID><尺寸后缀>.jpg" alt="-店名">
    </a>
  </div>
  <div class="picture-info">
    <div class="name"><h3><a class="J_entry" href="/photos/7605587866"></a></h3></div>
    <div class="info">
      <a rel="nofollow" href="/member/1978375941" title="流东向水春江一">流东向水春江一</a>
      <em class="sep">|</em>
      <span>25-12-18</span>
      <div class="digg-box"><a ... onclick="…return $PicReport(7605587866, 1978375941, '流东向水春江一');">报错</a></div>
    </div>
  </div>
</li>
```

由此得到三条可靠规则：

* **作者**：卡片内 `a[href^="/member/"]` 的文本或 `title`（备选：`$PicReport(图片ID, 用户ID, '用户名')`；用户 ID 为 `-1` 表示该图确实没有作者 —— 此时必须留空，不能猜）。
* **时间**：`em.sep` 后面的 `<span>`。
* **日期格式**：`25-12-18` 表示跨年（2025-12-18）；`06-21` 表示**当年**（点评只在跨年时才显示年份）。
  实测 160 个日期中 65 个是 `MM-DD`、95 个是 `YY-MM-DD`，**所有 `MM-DD` 的月日都不超过抓取当天的月日**，与「当年显示 MM-DD」完全吻合。

页面里还有一段内嵌 JSON：`'records': '{"img":[{"full":"原图","thumb":"小图","picId":7605587866}, …]}'`，
`picId` 与卡片的 `/photos/<id>` 一一对应（实测 3 家商户 16/16 完全一致）。

---

## 三、V3.0 怎么改

### 1) 只认「单图卡片」，绝不跨卡片取值
* 按 `li.J_list` → `.picture-info` →（兜底）`li/div` 里「只含一张图片」的祖先来定位卡片。
* 元数据只从**只包含这一张图片**的卡片里取；向上遇到含第二张图片的祖先立即停止。
* **删掉了「±5000 字符窗口」兜底**：匹配不到就留空。宁可空，也不要错。
* 未知版式仍可用：从 `<img>` 向上找最近的、只含一张图、且带 `/member/` 链接或日期的祖先（兄弟姐妹结构的作者/时间也能配对）。

### 2) 日期解析 + 年份补全
* 支持 `25-12-18`、`2024-07-15`、`2019年9月24日`、`2024/7/5 13:20`、`3天前` 等写法。
* `MM-DD` 按实测规则补成 `YYYY-MM-DD`（可关闭），同时保存：
  * `published_raw` —— 页面上的原始文本（如 `06-21`），便于核对；
  * `year_inferred` —— 标记年份是推算出来的（界面显示为 `2026-06-21＊`）。

### 3) 图片与元数据绑定更稳
卡片内 URL 精确匹配 → 不成再按「去掉 `@尺寸` 后缀的文件名」匹配，同一个文件换任何尺寸后缀都能对上。

### 4) 元数据带置信度写库，旧错值可被纠正
新增 `metadata_confidence`：置信度更高的一次解析可以覆盖旧值；同样或更低时只填空字段；空值默认不覆盖，但**置信度更高时允许清空**（用于纠正「匿名图片被写了隔壁作者」的历史错值）。

### 5) 依赖缺失不再静默
未安装 beautifulsoup4 时日志里明确报警并说明本次不写元数据，`--check-deps` 也能直接查。

### 6) 顺手修掉两个会毁数据的相邻问题
* **`<script>` 内嵌 JSON 的图片变体被当成独立图片**：实测一个 16 张照片的页面会产生 **48 个**待下载地址（原图 + 小图 + `.js`），现改为先剥掉 `<script>/<style>` 再扫图片（纯 JS 渲染的版式会自动回退到整页扫描），结果正好 16 个。
* **相册卡片里的 `<img>` 只有 240×180**，低于默认「最小边长 200」会被**整批当成过小跳过**。现改为按画质从高到低尝试：去掉尺寸后缀的地址（实测 240×180 → 1280×853）→ 页面内嵌 JSON 的 `full`（实测 240×180 → 700×525）→ 卡片原地址；失败自动回退。manifest 里的 `download_url` 列会记录实际下载地址。

### 7) 可一键修复历史错误数据
新增设置「**重采已完成页面以修正旧数据**」（默认关）。打开后重采 `status=completed` 的页面；
配合「跨页 URL 预去重」开启时**不会重新下载图片**，只重写作者/时间 —— 实测整页 16 条错误数据一次修好，下载调用 0 次。

---

## 四、验证结果（都是真实执行的，不是推测）

* `python verify_v3.py --live` → **85 项全部通过**，其中：
  * 3 个真实相册页（上海/郑州/长沙，含点评 CDN 与美团 CDN 两种图片源）：每页 16 张卡片、16 个图片地址、16 条内嵌原图数据；
  * **16/16 张都解析出时间**；
  * **作者数与页面里 `/member/` 链接数完全一致**（15 / 16 / 13 —— 少的那些确实是匿名或商户官方图，留空不猜）；
  * 每张图片都绑定了元数据，且结果里不再有 `.js/.css` 资源；
  * 旧数据修复流程：整页 16 条错误数据一次修正，0 次重新下载。
* 端到端跑 `crawl_page`（真实 HTML + 真实 requests/PIL/imagehash + 假图片传输层，覆盖 下载 / SHA256 重复 / 更高分辨率替换 / 尺寸过小 / 下载失败 全部分支）→ **34 项全部通过**。
* CLI 与界面装配（`--check-deps`、`--selftest`、`--parse-html`、缩略图渲染、悬停提示、19 列解包）→ **9 项全部通过**。
* 旧版 `archive.db` 自动补列（`photo_id / photo_index / published_raw / year_inferred / metadata_confidence`）已验证；旧的 `manifest.csv` 不会被改写，新版另存 `manifest_v3.csv`，避免列错位。

---

## 五、怎么用

```bat
pip install beautifulsoup4 requests pillow imagehash

:: 1. 先自检（离线，不联网）
python dianping_archive_collector_v3_1.py --check-deps
python dianping_archive_collector_v3_1.py --selftest

:: 2. 正常使用：直接运行就是原来的图形界面
python dianping_archive_collector_v3_1.py

:: 3. 想核对某一页解析得对不对（用已经保存下来的 page.html）
python dianping_archive_collector_v3_1.py --parse-html dianping_archive\<shop_id>\page_1\page.html
```

`--parse-html` 会打印每张图的：图片 ID / 作者 / 发布时间 / 原始文本 / 置信度 / 图片地址。
如果作者列是空的且置信度为 85，说明这张图页面上确实没有作者（匿名或商户官方图），这是正确结果。

**修复历史数据**：在「设置 → 上传者 / 发布时间」里勾选「重采已完成页面以修正旧数据」，然后按原方式采集同一商户即可（建议同时保持「跨页 URL 预去重」开启，这样不会重复下载图片）。修完可以再取消勾选。

**新增设置**（设置页 →「上传者 / 发布时间」）：

| 设置 | 默认 | 作用 |
|---|---|---|
| 补全 MM-DD 的年份 | 开 | `06-21` → `2026-06-21`，并保留原始文本与推算标记 |
| 下载原图（不用 240×180 缩略图） | 开 | 优先取页面内嵌原图，失败自动回退 |
| 重采已完成页面以修正旧数据 | 关 | 修复历史错误元数据，不重新下载 |

界面上的变化：缩略图说明里，推算出来的年份会带 `＊`；鼠标移到说明上会显示「原始时间 / 图片ID / 来源 / 置信度」；底部统计增加了「有作者 / 有时间 / 年份推算」三项。
