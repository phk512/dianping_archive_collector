# V3.2 新功能说明

脚本：`dianping_archive_collector_v3_2.py`（取代 V3.1 / V3.0，功能只增不减）
验证脚本：`verify_features.py`、`verify_login.py`、`verify_v3.py`、`verify_gui_cli.py`

V3.1 新增：采集日期范围、自动获取商户名称、自动找店（导入列表页 HTML）
**V3.2 新增：带登录态直接抓列表页/搜索页并自动翻页** —— 就是你说"还有改进空间"的那一块。

---

## 一、带登录态抓列表页（V3.2，推荐用法）

### 为什么需要登录态

实测结论（2026-09）：点评的**分类列表页、搜索结果页、商户主页、会员页**都有风控，
匿名请求会 302 跳到 `verify.meituan.com` 的验证中心；
**只有相册页 `/shop/<id>/photos` 不需要登录**。
所以在浏览器里登录之后，这些页面就能正常打开 —— 你的测试正好验证了这一点。

### 怎么用（界面上三步）

1. 在**已登录**的浏览器（Edge/Chrome 都行）里打开列表页，例如
   `https://www.dianping.com/shanghai/ch10`；
2. 按 `F12` → `Network` → 刷新页面 → 点列表里**第一个请求**（就是那个 ch10 文档请求）
   → 右键 →「复制」→「**以 cURL 格式复制**」；
3. 回到程序「自动找店」标签页，把内容粘贴进「Cookie / cURL」框 →
   点「**解析 cURL**」→ 点「**测试列表页**」确认能访问 → 设置城市/年限 → 「**开始寻找**」。

程序会自动：翻页抓取列表 → 解析出所有店铺 ID → 逐店核验（每店 2 次请求）→
按城市 + 估算经营年限 + 照片数筛选 → 结果列表可导出 CSV 或一键排队采集。

「最多翻页」默认 3 页（约 45~90 家店），「最多核验数」默认 300，核验间隔默认 1.2 秒 ——
别一次拉太大，慢慢来更安全。

### 命令行同样支持

```bat
:: 把「Copy as cURL」存成 txt，然后：
python dianping_archive_collector_v3_2.py --discover-url https://www.dianping.com/shanghai/ch10 ^
    --curl CopyAsCurl.txt --max-pages 3 --city 上海 --min-years 15 --min-photos 300 --limit 200

:: 也可以直接传 Cookie 字符串
python dianping_archive_collector_v3_2.py --discover-url <网址> --cookie "cy=8; cye=shanghai; ..."
```

### 关于登录态本身

* 只保存在本机：`dianping_settings.json` 里的 `cookie` 字段（明文），**只会发给大众点评**。
  不想留了随时点「**清除登录态**」按钮，或直接在设置页清空。
* 会**过期**（一般几天到几周）。过期后程序会明确提示
  「被点评风控拦截（跳到了验证中心）：Cookie 失效或未登录」，
  重新复制一次 cURL 即可。
* 粘贴时会把 UA、Referer 一起带过来，尽量贴近你浏览器的真实请求；
  如果点评哪天开始校验更严格的指纹（TLS/HTTP2），cURL 这条路可能失效 ——
  那就回到「另存为 HTML」的方式（下面第二种），它永远可用。

---

## 二、采集日期截止（自定义日期范围）

**设置页 → 分组「采集日期范围」**

| 设置 | 说明 |
|---|---|
| 起始日期（含） | 只收集这个日期**之后**的照片，留空表示不限 |
| 截止日期（含） | 只收集这个日期**之前**的照片，留空表示不限 |
| 日期未知的图片也收集 | 默认开。页面没给出日期的图片是否保留 |

写法很宽松：`2010`、`2010-06`、`2010-06-15`、`2010/6/5`、`2010年6月15日` 都能识别，
保存时规整成 `YYYY-MM-DD`（只写年份按 1 月 1 日算）。

**行为**

* 范围外的图片**不下载**，只在当页 `manifest.csv` 记一行 `skipped_by_date(日期)`。
* **整页都不在范围内时连页面一起跳过**（页面状态记 `skipped_by_date`），一次图片请求都不发。
  实测：截止 `2026-01-31` 时一页 16 张只下载 1 张；起始 `2026-08-01` 只下载 3 张；
  设成 `2030-01-01` 则整页跳过、0 次请求。
* 建议保持「补全 MM-DD 的年份」开启，否则页面只显示 `06-21` 的照片会被当成"日期未知"。

---

## 三、自动获取商户名称

以前档案库里永远是"未命名商户"。现在采集任何一页时会自动解析并写入：

| 字段 | 来源（按优先级） | 实测 |
|---|---|---|
| 商户名称 | `<title>名字-图片-城市-大众点评网</title>` → `<meta name="Keywords">` → 页头 `/shop/<id>` 链接文字 | 富贵面馆(镇坪路店) |
| 城市 | 标题里的城市段（兼容 `-第N页-`、名字里含 `-`） | 上海 |
| 相册照片总数 | 内嵌 `'albumPicCount': 3379` | 3379 |
| 最早照片日期 | 采集各页里最早的一张 | 2013-05-12 |

「档案浏览」显示：`上海 | 富贵面馆(镇坪路店) | 相册 3379 张 | 最早照片 2013-05-12（至少 13 年） | Shop ID: 9952743`，
左侧列表带 `· 至少13年`。命令行：`--probe 9952743`。

附带提速：**末页发现用内嵌照片总数直接算**（末页 = ⌈总数 ÷ 每页 16⌉，实测 5/5 精确），
3379 张的相册定位末页只要 **2 次请求**。

---

## 四、自动找店（三种来源）

| 来源 | 是否需要登录态 | 说明 |
|---|---|---|
| **列表页网址** | 需要（推荐） | 自动翻页，见第一节 |
| **本地 HTML 文件** | 不需要 | 浏览器打开列表页后 `Ctrl+S` 另存为「网页，仅 HTML」，导入即可；也可多选多个文件 |
| **粘贴文本 / 链接** | 不需要 | 分享文本、复制的店址列表都行；`www`/`m`/`shopinfo`/`share` 各种写法都认，自动去重 |
| **档案库已有商户** | 不需要 | 把已采集的店重新核验，补齐名称/城市/照片数/最早照片，并挑出其中的老店 |

无论哪种来源，**店铺 ID 的提取都不依赖具体版式**（只认 `/shop/<id>` 链接），
名称最终以相册页核验结果为准；列表页若带「15年老店」「老字号」字样，会作为"页面标注（仅提示）"记在备注里。

**「至少经营多少年」怎么来的（重要）**

点评不公开任何"开业时间/收录时间"字段。可行信号是：**相册"新→旧"有序，最后一页就是最老的照片**（实测 5 家商户）。

```
抓相册首页 → 照片总数 → 末页 = ⌈总数÷16⌉ → 抓末页 → 取其中最早的一张照片日期
至少经营年数 = 今天 − 最早照片日期
```

* 照片不可能早于开业，所以这是**保守下界**：显示"至少 13 年"就一定 ≥13 年，不会偏大。
* 反过来会漏：20 年老店如果照片都是最近 3 年传的，只会显示"至少 3 年"。
  实测海底捞(海宁路店)实际是老店，但相册最早只有 2016-12-15 → 约 9.8 年。
  **想宽松就把年限调小或设 0 只按城市筛。**

---

## 五、验证结果（都真实执行过）

| 验证脚本 | 结果 |
|---|---|
| `verify_login.py` | **34 项全过**：bash/cmd 两种「Copy as cURL」解析、翻页地址构造（路径式 `/p2`、参数式 `?pg=`、搜索页）、列表页卡片解析（`#shop-all-list` 结构 + 未知版式兜底）、`15年老店`/`老字号` 标注识别、`/shop/photos` 这类伪链接不误收、登录态注入与清除、风控页识别；**真实联网**：匿名访问分类页被正确识别为风控并给出可操作提示，且不影响相册核验链路 |
| `verify_features.py` | **69 项全过**：日期范围端到端（只下载范围内图片、整页跳过）、商户名/城市/照片数解析、末页 2 次请求命中、ID 提取、城市/年限/照片数筛选边界、候选入库与状态、多店采集去重 |
| `verify_v3.py --live` / 离线 | **85 / 54 项全过**（V3.0 的元数据修复回归，含 3 个真实页面） |
| `verify_gui_cli.py` | 全过：6 个命令行子命令（含 `--discover-url --curl`）、新标签页与候选表、「解析 cURL」写入设置与即时生效、「清除登录态」、界面显示「至少N年」 |

---

## 六、两点提醒

1. **礼貌抓取**：找店核验是每店 2 次请求（默认间隔 1.2 秒），列表页翻页之间也有延迟。
   别把「最多核验数/最多翻页」一次开太大；采集时保持页/图延迟。
2. **Cookie 是敏感信息**：它等于你的登录态。只在本机设置文件里保存、只发给点评；
   不要把它贴到聊天群或仓库里。用完可以点「清除登录态」。
